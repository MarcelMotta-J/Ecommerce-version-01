# Backlog

## Refatorações

- [ ] Mover `MercadoPagoPaymentClient` de `admin.service` para `payment.service`.
- [ ] Mover `MercadoPagoPaymentResponse` de `admin.dto` para `payment.dto`.
- [ ] Atualizar os imports no `PaymentController`.
- [ ] Confirmar que o webhook continua respondendo `200 OK`.
